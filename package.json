{
	"name": "@my-scope/neat-url",
	"version": "1.0.0",
	"license": "MIT",
	"main": "src/index.js",
	"repository": {
		"type": "git",
		"url": "https://github.com/x/x.git"
	},
	"scripts": {
		"test": "jest --coverage",
		"postpublish": "git push origin --all; git push origin --tags"
	},
	"devDependencies": {
		"jest": "^24.9.0"
	},
	"dependencies": {
		"psl": "^1.4.0",
		"qs": "^6.8.0"
	}
}
